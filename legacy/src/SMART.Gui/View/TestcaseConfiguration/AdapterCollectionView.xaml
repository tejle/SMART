<ContentControl x:Class="SMART.Gui.View.TestcaseConfiguration.AdapterCollectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    >
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" >
            <Grid DockPanel.Dock="Top">
                <TextBlock HorizontalAlignment="Left" Text="Adapters" FontFamily="Segoe UI" FontWeight="Medium" Foreground="Gray"  FontSize="12pt" />
                <Button 
                HorizontalAlignment="Right" 
                Style="{DynamicResource TitleBarButton}" 
                Content="{DynamicResource Horizon_Image_Exit}" 
                ToolTip="Close" 
                Command="{Binding Close}" />

            </Grid>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" >
            <Label Content="Available Adapters" />
            <ComboBox IsSynchronizedWithCurrentItem="True" 
                          SelectedItem="{Binding Path=CurrentSelectedAvailableAdapter, Mode=TwoWay}" 
                          DisplayMemberPath="Name"                          
                          ItemsSource="{Binding AvailableAdapters}" Width="300" Margin="0,0,10,0" />
            <Button Command="{Binding AddAdapter}" Content="Add" />
        </StackPanel>

        <ListBox                          
            Grid.Row="2"            
            Margin="0,10,0,0"
            SelectedItem="{Binding Path=CurrentAdapter, Mode=TwoWay}"
            IsSynchronizedWithCurrentItem="True"            
            SelectionMode="Single"
            HorizontalContentAlignment="Stretch" 
            BorderThickness="0"            
            Background="Transparent"
            ItemsSource="{Binding Adapters}" 
            ItemTemplate="{DynamicResource SettingsItemTemplate}"
            >
        </ListBox>

    </Grid>
</ContentControl>
